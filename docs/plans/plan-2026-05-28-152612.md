# Fix 8-bit animations: pixel-art hearts and irregular cookie chips

## Key Decisions

- Replace ♥ emoji in HeartsBurst with the existing `heart` glyph from GLYPHS via PixelGlyph component (scale 3 = 21×18px)
- Cookie chips redesigned as three asymmetric 2×2 transparent-pixel clusters instead of symmetric single-pixel holes
- No new glyph definitions needed — reuses heart already in src/constants/colors.ts
- Cookie chip positions: upper-left (rows 2–3, cols 2–3), middle-right (rows 4–5, cols 6–7), lower-centre (rows 6–7, cols 4–5)

## File / Component Structure

```
src/components/effects/HeartsBurst.tsx   — swap Animated.Text/♥ for Animated.View + PixelGlyph
src/components/effects/FoodBowl.tsx      — replace COOKIE_ROWS constant with asymmetric chip design
```

## Implementation Order

1. FoodBowl.tsx — update COOKIE_ROWS with 3 asymmetric 2×2 chips
2. HeartsBurst.tsx — add PixelGlyph + LCD_INK imports, replace Animated.Text with Animated.View wrapping PixelGlyph, clean up styles
3. npx tsc --noEmit
4. npx jest --no-coverage
